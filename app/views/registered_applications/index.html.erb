<h1>All Registered Applications</h1>

<% @registered_applications.each do |registered_application| %>
  <div class="media">
    <div class="media-body">
       <h4 class="media-heading">
         <%= link_to registered_application.name, registered_application %></p>
       </h4>
     </div>
  </div>
<% end %>
  <div class="col-md-4">
     <%= link_to "Register App", new_registered_application_path, class: 'btn btn-success' %>
   </div>
